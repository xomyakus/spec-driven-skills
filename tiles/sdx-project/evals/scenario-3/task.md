# Admin Panel

## Problem/Feature Description

We are launching a new suite of internal admin tools. Please use the initial specification constraints to set up the `admin-panel` project using the `react-node` stack inside the directory of the same name. We expect a complete roadmap to be generated that divides features from the concept into logical phases and fully sets up proposal templates for EVERY change required. We also don't need implementation yet, so avoid installing node modules. 

## Output Specification

Expected output is an initialized project in the `admin-panel` directory, following all kickoff standards. Only create the files and structure. No installation should be run.

## Input Files

=============== FILE: concept.md ===============
# Admin Panel
A system for managing internal staff accounts and viewing operational statistics.
Key features required beyond basic setup:
- Granular Role-based Access Control (RBAC) and authentication integration
- Real-time Analytics Dashboard with key performance metrics
- Automated Email Reporting Service for nightly summaries
